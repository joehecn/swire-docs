import{o as n,e as a,q as s,G as e,f as t,s as o}from"./framework.c00dda38.js";const p='{"title":"Migrating from Vue 2","description":"","frontmatter":{},"headers":[{"level":2,"title":"Breaking Changes","slug":"breaking-changes"},{"level":3,"title":"new Router becomes createRouter","slug":"new-router-becomes-createrouter"},{"level":3,"title":"New history option to replace mode","slug":"new-history-option-to-replace-mode"},{"level":3,"title":"Moved the base option","slug":"moved-the-base-option"},{"level":3,"title":"Removed * (star or catch all) routes","slug":"removed-star-or-catch-all-routes"},{"level":3,"title":"Replaced onReady with isReady","slug":"replaced-onready-with-isready"},{"level":3,"title":"scrollBehavior changes","slug":"scrollbehavior-changes"},{"level":3,"title":"<router-view>, <keep-alive>, and <transition>","slug":"router-view-keep-alive-and-transition"},{"level":3,"title":"Removal of append prop in <router-link>","slug":"removal-of-append-prop-in-router-link"},{"level":3,"title":"Removal of event and tag props in <router-link>","slug":"removal-of-event-and-tag-props-in-router-link"},{"level":3,"title":"Removal of the exact prop in <router-link>","slug":"removal-of-the-exact-prop-in-router-link"},{"level":3,"title":"Navigation guards in mixins are ignored","slug":"navigation-guards-in-mixins-are-ignored"},{"level":3,"title":"Removal of router.match and changes to router.resolve","slug":"removal-of-router-match-and-changes-to-router-resolve"},{"level":3,"title":"Removal of router.getMatchedComponents()","slug":"removal-of-router-getmatchedcomponents"},{"level":3,"title":"All navigations are now always asynchronous","slug":"all-navigations-are-now-always-asynchronous"},{"level":3,"title":"Passing content to route components\' <slot>","slug":"passing-content-to-route-components-slot"},{"level":3,"title":"Removal of parent from route locations","slug":"removal-of-parent-from-route-locations"},{"level":3,"title":"Removal of pathToRegexpOptions","slug":"removal-of-pathtoregexpoptions"},{"level":3,"title":"Removal of unnamed parameters","slug":"removal-of-unnamed-parameters"},{"level":3,"title":"Usage of history.state","slug":"usage-of-history-state"},{"level":3,"title":"routes option is required in options","slug":"routes-option-is-required-in-options"},{"level":3,"title":"Non existent named routes","slug":"non-existent-named-routes"},{"level":3,"title":"Missing required params on named routes","slug":"missing-required-params-on-named-routes"},{"level":3,"title":"Named children routes with an empty path no longer appends a slash","slug":"named-children-routes-with-an-empty-path-no-longer-appends-a-slash"},{"level":3,"title":"$route properties Encoding","slug":"route-properties-encoding"},{"level":3,"title":"TypeScript changes","slug":"typescript-changes"},{"level":2,"title":"New Features","slug":"new-features"}],"relativePath":"guide/migration/index.md","lastUpdated":1607924286800.615}';var c={};const r=e('<h1 id="migrating-from-vue-2"><a class="header-anchor" href="#migrating-from-vue-2" aria-hidden="true">#</a> Migrating from Vue 2</h1><p>Most of Vue Router API has remained unchanged during its rewrite from v3 (for Vue 2) to v4 (for Vue 3) but there are still a few breaking changes that you might encounter while migrating your application. This guide is here to help you understand why these changes happened and how to adapt your application to make it work with Vue Router 4.</p><h2 id="breaking-changes"><a class="header-anchor" href="#breaking-changes" aria-hidden="true">#</a> Breaking Changes</h2><p>Changes are ordered by their usage. It is therefore recommended to follow this list in order.</p><h3 id="new-router-becomes-createrouter"><a class="header-anchor" href="#new-router-becomes-createrouter" aria-hidden="true">#</a> new Router becomes createRouter</h3><p>Vue Router is no longer a class but a set of functions. Instead of writing <code>new Router()</code>, you now have to call <code>createRouter</code>:</p><div class="language-js"><pre><code><span class="token comment">// previously was</span>\n<span class="token comment">// import Router from &#39;vue-router&#39;</span>\n<span class="token keyword">import</span> <span class="token punctuation">{</span> createRouter <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&#39;vue-router&#39;</span>\n\n<span class="token keyword">const</span> router <span class="token operator">=</span> <span class="token function">createRouter</span><span class="token punctuation">(</span><span class="token punctuation">{</span>\n  <span class="token comment">// ...</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span>\n</code></pre></div><h3 id="new-history-option-to-replace-mode"><a class="header-anchor" href="#new-history-option-to-replace-mode" aria-hidden="true">#</a> New <code>history</code> option to replace <code>mode</code></h3><p>The <code>mode: &#39;history&#39;</code> option has been replaced with a more flexible one named <code>history</code>. Depending on which mode you were using, you will have to replace it with the appropriate function:</p><ul><li><code>&quot;history&quot;</code>: <code>createWebHistory()</code></li><li><code>&quot;hash&quot;</code>: <code>createWebHashHistory()</code></li><li><code>&quot;abstract&quot;</code>: <code>createMemoryHistory()</code></li></ul><p>Here is a full snippet:</p><div class="language-js"><pre><code><span class="token keyword">import</span> <span class="token punctuation">{</span> createRouter<span class="token punctuation">,</span> createWebHistory <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&#39;vue-router&#39;</span>\n<span class="token comment">// there is also createWebHashHistory and createMemoryHistory</span>\n\n<span class="token function">createRouter</span><span class="token punctuation">(</span><span class="token punctuation">{</span>\n  history<span class="token operator">:</span> <span class="token function">createWebHistory</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span>\n  routes<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span>\n</code></pre></div><p>On SSR, you need to manually pass the appropriate history:</p><div class="language-js"><pre><code><span class="token comment">// router.js</span>\n<span class="token keyword">let</span> history <span class="token operator">=</span> isServer <span class="token operator">?</span> <span class="token function">createMemoryHistory</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">:</span> <span class="token function">createWebHistory</span><span class="token punctuation">(</span><span class="token punctuation">)</span>\n<span class="token keyword">let</span> router <span class="token operator">=</span> <span class="token function">createRouter</span><span class="token punctuation">(</span><span class="token punctuation">{</span> routes<span class="token punctuation">,</span> history <span class="token punctuation">}</span><span class="token punctuation">)</span>\n<span class="token comment">// somewhere in your server-entry.js</span>\nrouter<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span>req<span class="token punctuation">.</span>url<span class="token punctuation">)</span> <span class="token comment">// request url</span>\nrouter<span class="token punctuation">.</span><span class="token function">isReady</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">then</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>\n  <span class="token comment">// resolve the request</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span>\n</code></pre></div><p><strong>Reason</strong>: enable tree shaking of non used histories as well as implementing custom histories for advanced use cases like native solutions.</p><h3 id="moved-the-base-option"><a class="header-anchor" href="#moved-the-base-option" aria-hidden="true">#</a> Moved the <code>base</code> option</h3><p>The <code>base</code> option is now passed as the first argument to <code>createWebHistory</code> (and other histories):</p><div class="language-js"><pre><code><span class="token keyword">import</span> <span class="token punctuation">{</span> createRouter<span class="token punctuation">,</span> createWebHistory <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&#39;vue-router&#39;</span>\n<span class="token function">createRouter</span><span class="token punctuation">(</span><span class="token punctuation">{</span>\n  history<span class="token operator">:</span> <span class="token function">createWebHistory</span><span class="token punctuation">(</span><span class="token string">&#39;/base-directory/&#39;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>\n  routes<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span>\n</code></pre></div><h3 id="removed-star-or-catch-all-routes"><a class="header-anchor" href="#removed-star-or-catch-all-routes" aria-hidden="true">#</a> Removed <code>*</code> (star or catch all) routes</h3><p>Catch all routes (<code>*</code>, <code>/*</code>) must now be defined using a parameter with a custom regex:</p><div class="language-js"><pre><code><span class="token keyword">const</span> routes <span class="token operator">=</span> <span class="token punctuation">[</span>\n  <span class="token comment">// pathMatch is the name of the param, e.g., going to /not/found yields</span>\n  <span class="token comment">// { params: { pathMatch: [&#39;not&#39;, &#39;found&#39;] }}</span>\n  <span class="token comment">// this is thanks to the last *, meaning repeated params and it is necessary if you</span>\n  <span class="token comment">// plan on directly navigating to the not-found route using its name</span>\n  <span class="token punctuation">{</span> path<span class="token operator">:</span> <span class="token string">&#39;/:pathMatch(.*)*&#39;</span><span class="token punctuation">,</span> name<span class="token operator">:</span> <span class="token string">&#39;not-found&#39;</span><span class="token punctuation">,</span> component<span class="token operator">:</span> NotFound <span class="token punctuation">}</span><span class="token punctuation">,</span>\n  <span class="token comment">// if you omit the last `*`, the `/` character in params will be encoded when resolving or pushing</span>\n  <span class="token punctuation">{</span> path<span class="token operator">:</span> <span class="token string">&#39;/:pathMatch(.*)&#39;</span><span class="token punctuation">,</span> name<span class="token operator">:</span> <span class="token string">&#39;bad-not-found&#39;</span><span class="token punctuation">,</span> component<span class="token operator">:</span> NotFound <span class="token punctuation">}</span><span class="token punctuation">,</span>\n<span class="token punctuation">]</span>\n<span class="token comment">// bad example if using named routes:</span>\nrouter<span class="token punctuation">.</span><span class="token function">resolve</span><span class="token punctuation">(</span><span class="token punctuation">{</span>\n  name<span class="token operator">:</span> <span class="token string">&#39;bad-not-found&#39;</span><span class="token punctuation">,</span>\n  params<span class="token operator">:</span> <span class="token punctuation">{</span> pathMatch<span class="token operator">:</span> <span class="token string">&#39;not/found&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">.</span>href <span class="token comment">// &#39;/not%2Ffound&#39;</span>\n<span class="token comment">// good example:</span>\nrouter<span class="token punctuation">.</span><span class="token function">resolve</span><span class="token punctuation">(</span><span class="token punctuation">{</span>\n  name<span class="token operator">:</span> <span class="token string">&#39;not-found&#39;</span><span class="token punctuation">,</span>\n  params<span class="token operator">:</span> <span class="token punctuation">{</span> pathMatch<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token string">&#39;not&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;found&#39;</span><span class="token punctuation">]</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>\n<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">.</span>href <span class="token comment">// &#39;/not/found&#39;</span>\n</code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>You don&#39;t need to add the <code>*</code> for repeated params if you don&#39;t plan to directly push to the not found route using its name. If you call <code>router.push(&#39;/not/found/url&#39;)</code>, it will provide the right <code>pathMatch</code> param.</p></div><p><strong>Reason</strong>: Vue Router doesn&#39;t use <code>path-to-regexp</code> anymore, instead it implements its own parsing system that allows route ranking and enables dynamic routing. Since we usually add one single catch-all route per project, there is no big benefit in supporting a special syntax for <code>*</code>. The encoding of params is encoding across routes, without exception to make things easier to predict.</p><h3 id="replaced-onready-with-isready"><a class="header-anchor" href="#replaced-onready-with-isready" aria-hidden="true">#</a> Replaced <code>onReady</code> with <code>isReady</code></h3><p>The existing <code>router.onReady()</code> function has been replaced with <code>router.isReady()</code> which doesn&#39;t take any argument and returns a Promise:</p><div class="language-js"><pre><code><span class="token comment">// replace</span>\nrouter<span class="token punctuation">.</span><span class="token function">onReady</span><span class="token punctuation">(</span>onSuccess<span class="token punctuation">,</span> onError<span class="token punctuation">)</span>\n<span class="token comment">// with</span>\nrouter<span class="token punctuation">.</span><span class="token function">isReady</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">then</span><span class="token punctuation">(</span>onSuccess<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">catch</span><span class="token punctuation">(</span>onError<span class="token punctuation">)</span>\n<span class="token comment">// or use await:</span>\n<span class="token keyword">try</span> <span class="token punctuation">{</span>\n  <span class="token keyword">await</span> router<span class="token punctuation">.</span><span class="token function">isReady</span><span class="token punctuation">(</span><span class="token punctuation">)</span>\n  <span class="token comment">// onSuccess</span>\n<span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span>err<span class="token punctuation">)</span> <span class="token punctuation">{</span>\n  <span class="token comment">// onError</span>\n<span class="token punctuation">}</span>\n</code></pre></div><h3 id="scrollbehavior-changes"><a class="header-anchor" href="#scrollbehavior-changes" aria-hidden="true">#</a> <code>scrollBehavior</code> changes</h3><p>The object returned in <code>scrollBehavior</code> is now similar to <a href="https://developer.mozilla.org/en-US/docs/Web/API/ScrollToOptions" target="_blank" rel="noopener noreferrer"><code>ScrollToOptions</code></a>: <code>x</code> is renamed to <code>left</code> and <code>y</code> is renamed to <code>top</code>. See <a href="https://github.com/vuejs/rfcs/blob/master/active-rfcs/0035-router-scroll-position.md" target="_blank" rel="noopener noreferrer">RFC</a>.</p><p><strong>Reason</strong>: making the object similar to <code>ScrollToOptions</code> to make it feel more familiar with native JS APIs and potentially enable future new options.</p><h3 id="router-view-keep-alive-and-transition"><a class="header-anchor" href="#router-view-keep-alive-and-transition" aria-hidden="true">#</a> <code>&lt;router-view&gt;</code>, <code>&lt;keep-alive&gt;</code>, and <code>&lt;transition&gt;</code></h3><p><code>transition</code> and <code>keep-alive</code> must now be used <strong>inside</strong> of <code>RouterView</code> via the <code>v-slot</code> API:</p><div class="language-vue"><pre><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>router-view</span> <span class="token attr-name">v-slot</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>{ Component }<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>transition</span><span class="token punctuation">&gt;</span></span>\n    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>keep-alive</span><span class="token punctuation">&gt;</span></span>\n      <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>component</span> <span class="token attr-name">:is</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>Component<span class="token punctuation">&quot;</span></span> <span class="token punctuation">/&gt;</span></span>\n    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>keep-alive</span><span class="token punctuation">&gt;</span></span>\n  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>transition</span><span class="token punctuation">&gt;</span></span>\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>router-view</span><span class="token punctuation">&gt;</span></span>\n</code></pre></div><p><strong>Reason</strong>: This was a necessary change. See the <a href="https://github.com/vuejs/rfcs/blob/master/active-rfcs/0034-router-view-keep-alive-transitions.md" target="_blank" rel="noopener noreferrer">related RFC</a>.</p><h3 id="removal-of-append-prop-in-router-link"><a class="header-anchor" href="#removal-of-append-prop-in-router-link" aria-hidden="true">#</a> Removal of <code>append</code> prop in <code>&lt;router-link&gt;</code></h3><p>The <code>append</code> prop has been removed from <code>&lt;router-link&gt;</code>. You can manually concatenate the value to an existing <code>path</code> instead:</p><div class="language-html"><pre><code>replace\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>router-link</span> <span class="token attr-name">to</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>child-route<span class="token punctuation">&quot;</span></span> <span class="token attr-name">append</span><span class="token punctuation">&gt;</span></span>to relative child<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>router-link</span><span class="token punctuation">&gt;</span></span>\nwith\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>router-link</span> <span class="token attr-name">:to</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>append($route.path, <span class="token punctuation">&#39;</span>child-route<span class="token punctuation">&#39;</span>)<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n  to relative child\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>router-link</span><span class="token punctuation">&gt;</span></span>\n</code></pre></div><p>You must define a global <code>append</code> function on your <em>App</em> instance:</p><div class="language-js"><pre><code>app<span class="token punctuation">.</span>config<span class="token punctuation">.</span>globalProperties<span class="token punctuation">.</span><span class="token function-variable function">append</span> <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token parameter">path<span class="token punctuation">,</span> pathToAppend</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span>\n  path <span class="token operator">+</span> <span class="token punctuation">(</span>path<span class="token punctuation">.</span><span class="token function">endsWith</span><span class="token punctuation">(</span><span class="token string">&#39;/&#39;</span><span class="token punctuation">)</span> <span class="token operator">?</span> <span class="token string">&#39;&#39;</span> <span class="token operator">:</span> <span class="token string">&#39;/&#39;</span><span class="token punctuation">)</span> <span class="token operator">+</span> pathToAppend\n</code></pre></div><p><strong>Reason</strong>: <code>append</code> wasn&#39;t used very often, is easy to replicate in user land.</p><h3 id="removal-of-event-and-tag-props-in-router-link"><a class="header-anchor" href="#removal-of-event-and-tag-props-in-router-link" aria-hidden="true">#</a> Removal of <code>event</code> and <code>tag</code> props in <code>&lt;router-link&gt;</code></h3><p>Both <code>event</code>, and <code>tag</code> props have been removed from <code>&lt;router-link&gt;</code>. You can use the <a href="/api/#router-link-s-v-slot"><code>v-slot</code> API</a> to fully customize <code>&lt;router-link&gt;</code>:</p><div class="language-html"><pre><code>replace\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>router-link</span> <span class="token attr-name">to</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>/about<span class="token punctuation">&quot;</span></span> <span class="token attr-name">tag</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>span<span class="token punctuation">&quot;</span></span> <span class="token attr-name">event</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>dblclick<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>About Us<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>router-link</span><span class="token punctuation">&gt;</span></span>\nwith\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>router-link</span> <span class="token attr-name">to</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>/about<span class="token punctuation">&quot;</span></span> <span class="token attr-name">custom</span> <span class="token attr-name">v-slot</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>{ navigate }<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>span</span> <span class="token attr-name">@click</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>navigate<span class="token punctuation">&quot;</span></span> <span class="token attr-name">@keypress.enter</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>navigate<span class="token punctuation">&quot;</span></span> <span class="token attr-name">role</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>link<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>About Us<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>span</span><span class="token punctuation">&gt;</span></span>\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>router-link</span><span class="token punctuation">&gt;</span></span>\n</code></pre></div><p><strong>Reason</strong>: These props were often used together to use something different from an <code>&lt;a&gt;</code> tag but were introduced before the <code>v-slot</code> API and are not used enough to justify adding to the bundle size for everybody.</p><h3 id="removal-of-the-exact-prop-in-router-link"><a class="header-anchor" href="#removal-of-the-exact-prop-in-router-link" aria-hidden="true">#</a> Removal of the <code>exact</code> prop in <code>&lt;router-link&gt;</code></h3><p>The <code>exact</code> prop has been removed because the caveat it was fixing is no longer present so you should be able to safely remove it. There are however two things you should be aware of:</p><ul><li>Routes are now active based on the route records they represent instead of the generated route location objects and their <code>path</code>, <code>query</code>, and <code>hash</code> properties</li><li>Only the <code>path</code> section is matched, <code>query</code>, and <code>hash</code> aren&#39;t taken into account anymore</li></ul><p>If you wish to customize this behavior, e.g. take into account the <code>hash</code> section, you should use the <a href="https://next.router.vuejs.org/api/#router-link-s-v-slot" target="_blank" rel="noopener noreferrer"><code>v-slot</code> API</a> to extend <code>&lt;router-link&gt;</code>.</p><p><strong>Reason</strong>: See the <a href="https://github.com/vuejs/rfcs/blob/master/active-rfcs/0028-router-active-link.md#summary" target="_blank" rel="noopener noreferrer">RFC about active matching</a> changes for more details.</p><h3 id="navigation-guards-in-mixins-are-ignored"><a class="header-anchor" href="#navigation-guards-in-mixins-are-ignored" aria-hidden="true">#</a> Navigation guards in mixins are ignored</h3><p>At the moment navigation guards in mixins are not supported. You can track its support at <a href="https://github.com/vuejs/vue-router-next/issues/454" target="_blank" rel="noopener noreferrer">vue-router#454</a>.</p><h3 id="removal-of-router-match-and-changes-to-router-resolve"><a class="header-anchor" href="#removal-of-router-match-and-changes-to-router-resolve" aria-hidden="true">#</a> Removal of <code>router.match</code> and changes to <code>router.resolve</code></h3><p>Both <code>router.match</code>, and <code>router.resolve</code> have been merged together into <code>router.resolve</code> with a slightly different signature. <a href="/api/#resolve">Refer to the API</a> for more details.</p><p><strong>Reason</strong>: Uniting multiple methods that were used for the same purpose.</p><h3 id="removal-of-router-getmatchedcomponents"><a class="header-anchor" href="#removal-of-router-getmatchedcomponents" aria-hidden="true">#</a> Removal of <code>router.getMatchedComponents()</code></h3><p>The method <code>router.getMatchedComponents</code> is now removed as matched components can be retrieved from <code>router.currentRoute.value.matched</code>:</p><div class="language-js"><pre><code>router<span class="token punctuation">.</span>currentRoute<span class="token punctuation">.</span>value<span class="token punctuation">.</span>matched<span class="token punctuation">.</span><span class="token function">flatMap</span><span class="token punctuation">(</span><span class="token parameter">record</span> <span class="token operator">=&gt;</span>\n  Object<span class="token punctuation">.</span><span class="token function">values</span><span class="token punctuation">(</span>record<span class="token punctuation">.</span>components<span class="token punctuation">)</span>\n<span class="token punctuation">)</span>\n</code></pre></div><p><strong>Reason</strong>: This method was only used during SSR and is a one liner that can be done by the user.</p><h3 id="all-navigations-are-now-always-asynchronous"><a class="header-anchor" href="#all-navigations-are-now-always-asynchronous" aria-hidden="true">#</a> <strong>All</strong> navigations are now always asynchronous</h3><p>All navigations, including the first one, are now asynchronous, meaning that, if you use a <code>transition</code>, you may need to wait for the router to be <em>ready</em> before mounting the app:</p><div class="language-js"><pre><code>app<span class="token punctuation">.</span><span class="token function">use</span><span class="token punctuation">(</span>router<span class="token punctuation">)</span>\n<span class="token comment">// Note: on Server Side, you need to manually push the initial location</span>\nrouter<span class="token punctuation">.</span><span class="token function">isReady</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">then</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> app<span class="token punctuation">.</span><span class="token function">mount</span><span class="token punctuation">(</span><span class="token string">&#39;#app&#39;</span><span class="token punctuation">)</span><span class="token punctuation">)</span>\n</code></pre></div><p>Otherwise there will be an initial transition as if you provided the <code>appear</code> prop to <code>transition</code> because the router displays its initial location (nothing) and then displays the first location.</p><p>Note that <strong>if you have navigation guards upon the initial navigation</strong>, you might not want to block the app render until they are resolved unless you are doing Server Side Rendering. In this scenario, not waiting the router to be ready to mount the app would yield the same result as in Vue 2.</p><h3 id="passing-content-to-route-components-slot"><a class="header-anchor" href="#passing-content-to-route-components-slot" aria-hidden="true">#</a> Passing content to route components&#39; <code>&lt;slot&gt;</code></h3><p>Before you could directly pass a template to be rendered by a route components&#39; <code>&lt;slot&gt;</code> by nesting it under a <code>&lt;router-view&gt;</code> component:</p><div class="language-html"><pre><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>router-view</span><span class="token punctuation">&gt;</span></span>\n  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>p</span><span class="token punctuation">&gt;</span></span>In Vue Router 3, I render inside the route component<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>p</span><span class="token punctuation">&gt;</span></span>\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>router-view</span><span class="token punctuation">&gt;</span></span>\n</code></pre></div><p>Because of the introduction of the <code>v-slot</code> api for <code>&lt;router-view&gt;</code>, you must pass it to the <code>&lt;component&gt;</code> using the <code>v-slot</code> API:</p><div class="language-html"><pre><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>router-view</span> <span class="token attr-name">v-slot</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>{ Component }<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>component</span> <span class="token attr-name">:is</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>Component<span class="token punctuation">&quot;</span></span><span class="token punctuation">&gt;</span></span>\n    <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>p</span><span class="token punctuation">&gt;</span></span>In Vue Router 3, I render inside the route component<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>p</span><span class="token punctuation">&gt;</span></span>\n  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>component</span><span class="token punctuation">&gt;</span></span>\n<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>router-view</span><span class="token punctuation">&gt;</span></span>\n</code></pre></div><h3 id="removal-of-parent-from-route-locations"><a class="header-anchor" href="#removal-of-parent-from-route-locations" aria-hidden="true">#</a> Removal of <code>parent</code> from route locations</h3><p>The <code>parent</code> property has been removed from normalized route locations (<code>this.$route</code> and object returned by <code>router.resolve</code>). You can still access it via the <code>matched</code> array:</p><div class="language-js"><pre><code><span class="token keyword">const</span> parent <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>$route<span class="token punctuation">.</span>matched<span class="token punctuation">[</span><span class="token keyword">this</span><span class="token punctuation">.</span>$route<span class="token punctuation">.</span>matched<span class="token punctuation">.</span>length <span class="token operator">-</span> <span class="token number">2</span><span class="token punctuation">]</span>\n</code></pre></div><p><strong>Reason</strong>: Having <code>parent</code> and <code>children</code> creates unnecessary circular references while the properties could be retrieved already through <code>matched</code>.</p><h3 id="removal-of-pathtoregexpoptions"><a class="header-anchor" href="#removal-of-pathtoregexpoptions" aria-hidden="true">#</a> Removal of <code>pathToRegexpOptions</code></h3><p>The <code>pathToRegexpOptions</code> and <code>caseSensitive</code> properties of route records have been replaced with <code>sensitive</code> and <code>strict</code> options for <code>createRouter()</code>. They can now also be directly passed when creating the router with <code>createRouter()</code>. Any other option specific to <code>path-to-regexp</code> has been removed as <code>path-to-regexp</code> is no longer used to parse paths.</p><h3 id="removal-of-unnamed-parameters"><a class="header-anchor" href="#removal-of-unnamed-parameters" aria-hidden="true">#</a> Removal of unnamed parameters</h3><p>Due to the removal of <code>path-to-regexp</code>, unnamed parameters are no longer supported:</p><ul><li><code>/foo(/foo)?/suffix</code> becomes <code>/foo/:_(foo)?/suffix</code></li><li><code>/foo(foo)?</code> becomes <code>/foo:_(foo)?</code></li><li><code>/foo/(.*)</code> becomes <code>/foo/:_(.*)</code></li></ul><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Note you can use any name instead of <code>_</code> for the param. The point is to provide one.</p></div><h3 id="usage-of-history-state"><a class="header-anchor" href="#usage-of-history-state" aria-hidden="true">#</a> Usage of <code>history.state</code></h3><p>Vue Router saves information on the <code>history.state</code>. If you have any code manually calling <code>history.pushState()</code>, you should likely avoid it or refactor it with a regular <code>router.push()</code> and a <code>history.replaceState()</code>:</p><div class="language-js"><pre><code><span class="token comment">// replace</span>\nhistory<span class="token punctuation">.</span><span class="token function">pushState</span><span class="token punctuation">(</span>myState<span class="token punctuation">,</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">,</span> url<span class="token punctuation">)</span>\n<span class="token comment">// with</span>\n<span class="token keyword">await</span> router<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span>url<span class="token punctuation">)</span>\nhistory<span class="token punctuation">.</span><span class="token function">replaceState</span><span class="token punctuation">(</span><span class="token punctuation">{</span> <span class="token operator">...</span>history<span class="token punctuation">.</span>state<span class="token punctuation">,</span> <span class="token operator">...</span>myState <span class="token punctuation">}</span><span class="token punctuation">,</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">)</span>\n</code></pre></div><p>Similarly, if you were calling <code>history.replaceState()</code> without preserving the current state, you will need to pass the current <code>history.state</code>:</p><div class="language-js"><pre><code><span class="token comment">// replace</span>\nhistory<span class="token punctuation">.</span><span class="token function">replaceState</span><span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token punctuation">}</span><span class="token punctuation">,</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">,</span> url<span class="token punctuation">)</span>\n<span class="token comment">// with</span>\nhistory<span class="token punctuation">.</span><span class="token function">replaceState</span><span class="token punctuation">(</span>history<span class="token punctuation">.</span>state<span class="token punctuation">,</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">,</span> url<span class="token punctuation">)</span>\n</code></pre></div><p><strong>Reason</strong>: We use the history state to save information about the navigation like the scroll position, previous location, etc.</p><h3 id="routes-option-is-required-in-options"><a class="header-anchor" href="#routes-option-is-required-in-options" aria-hidden="true">#</a> <code>routes</code> option is required in <code>options</code></h3><p>The property <code>routes</code> is now required in <code>options</code>.</p><div class="language-js"><pre><code><span class="token function">createRouter</span><span class="token punctuation">(</span><span class="token punctuation">{</span> routes<span class="token operator">:</span> <span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>\n</code></pre></div><p><strong>Reason</strong>: The router is designed to be created with routes even though you can add them later on. You need at least one route in most scenarios and this is written once per app in general.</p><h3 id="non-existent-named-routes"><a class="header-anchor" href="#non-existent-named-routes" aria-hidden="true">#</a> Non existent named routes</h3><p>Pushing or resolving a non existent named route throws an error:</p><div class="language-js"><pre><code><span class="token comment">// Oops, we made a typo in name</span>\nrouter<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span><span class="token punctuation">{</span> name<span class="token operator">:</span> <span class="token string">&#39;homee&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token comment">// throws</span>\nrouter<span class="token punctuation">.</span><span class="token function">resolve</span><span class="token punctuation">(</span><span class="token punctuation">{</span> name<span class="token operator">:</span> <span class="token string">&#39;homee&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token comment">// throws</span>\n</code></pre></div><p><strong>Reason</strong>: Previously, the router would navigate to <code>/</code> but display nothing (instead of the home page). Throwing an error makes more sense because we cannot produce a valid URL to navigate to.</p><h3 id="missing-required-params-on-named-routes"><a class="header-anchor" href="#missing-required-params-on-named-routes" aria-hidden="true">#</a> Missing required <code>params</code> on named routes</h3><p>Pushing or resolving a named route without its required params will throw an error:</p><div class="language-js"><pre><code><span class="token comment">// given the following route:</span>\n<span class="token keyword">const</span> routes <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">{</span> path<span class="token operator">:</span> <span class="token string">&#39;/users/:id&#39;</span><span class="token punctuation">,</span> name<span class="token operator">:</span> <span class="token string">&#39;user&#39;</span><span class="token punctuation">,</span> component<span class="token operator">:</span> UserDetails <span class="token punctuation">}</span><span class="token punctuation">]</span>\n\n<span class="token comment">// Missing the `id` param will fail</span>\nrouter<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span><span class="token punctuation">{</span> name<span class="token operator">:</span> <span class="token string">&#39;user&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>\nrouter<span class="token punctuation">.</span><span class="token function">resolve</span><span class="token punctuation">(</span><span class="token punctuation">{</span> name<span class="token operator">:</span> <span class="token string">&#39;user&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>\n</code></pre></div><p><strong>Reason</strong>: Same as above.</p><h3 id="named-children-routes-with-an-empty-path-no-longer-appends-a-slash"><a class="header-anchor" href="#named-children-routes-with-an-empty-path-no-longer-appends-a-slash" aria-hidden="true">#</a> Named children routes with an empty <code>path</code> no longer appends a slash</h3><p>Given any nested named route with an empty <code>path</code>:</p><div class="language-js"><pre><code><span class="token keyword">const</span> routes <span class="token operator">=</span> <span class="token punctuation">[</span>\n  <span class="token punctuation">{</span>\n    path<span class="token operator">:</span> <span class="token string">&#39;/dashboard&#39;</span><span class="token punctuation">,</span>\n    name<span class="token operator">:</span> <span class="token string">&#39;dashboard-parent&#39;</span><span class="token punctuation">,</span>\n    component<span class="token operator">:</span> DashboardParent\n    children<span class="token operator">:</span> <span class="token punctuation">[</span>\n      <span class="token punctuation">{</span> path<span class="token operator">:</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">,</span> name<span class="token operator">:</span> <span class="token string">&#39;dashboard&#39;</span><span class="token punctuation">,</span> component<span class="token operator">:</span> DashboardDefault <span class="token punctuation">}</span><span class="token punctuation">,</span>\n      <span class="token punctuation">{</span> path<span class="token operator">:</span> <span class="token string">&#39;settings&#39;</span><span class="token punctuation">,</span> name<span class="token operator">:</span> <span class="token string">&#39;dashboard-settings&#39;</span><span class="token punctuation">,</span> component<span class="token operator">:</span> DashboardSettings <span class="token punctuation">}</span><span class="token punctuation">,</span>\n    <span class="token punctuation">]</span><span class="token punctuation">,</span>\n  <span class="token punctuation">}</span><span class="token punctuation">,</span>\n<span class="token punctuation">]</span>\n</code></pre></div><p>Navigating or resolving to the named route <code>dashboard</code> will now produce a URL <strong>without a trailing slash</strong>:</p><div class="language-js"><pre><code>router<span class="token punctuation">.</span><span class="token function">resolve</span><span class="token punctuation">(</span><span class="token punctuation">{</span> name<span class="token operator">:</span> <span class="token string">&#39;dashboard&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">.</span>href <span class="token comment">// &#39;/dashboard&#39;</span>\n</code></pre></div><p>This has an important side effect about children <code>redirect</code> records like these:</p><div class="language-js"><pre><code><span class="token keyword">const</span> routes <span class="token operator">=</span> <span class="token punctuation">[</span>\n  <span class="token punctuation">{</span>\n    path<span class="token operator">:</span> <span class="token string">&#39;/parent&#39;</span><span class="token punctuation">,</span>\n    component<span class="token operator">:</span> Parent<span class="token punctuation">,</span>\n    children<span class="token operator">:</span> <span class="token punctuation">[</span>\n      <span class="token comment">// this would now redirect to `/home` instead of `/parent/home`</span>\n      <span class="token punctuation">{</span> path<span class="token operator">:</span> <span class="token string">&#39;&#39;</span><span class="token punctuation">,</span> redirect<span class="token operator">:</span> <span class="token string">&#39;home&#39;</span> <span class="token punctuation">}</span><span class="token punctuation">,</span>\n      <span class="token punctuation">{</span> path<span class="token operator">:</span> <span class="token string">&#39;home&#39;</span><span class="token punctuation">,</span> component<span class="token operator">:</span> Home <span class="token punctuation">}</span><span class="token punctuation">,</span>\n    <span class="token punctuation">]</span><span class="token punctuation">,</span>\n  <span class="token punctuation">}</span><span class="token punctuation">,</span>\n<span class="token punctuation">]</span>\n</code></pre></div><p>Note this will work if <code>path</code> was <code>/parent/</code> as the relative location <code>home</code> to <code>/parent/</code> is indeed <code>/parent/home</code> but the relative location of <code>home</code> to <code>/parent</code> is <code>/home</code>.</p>',103),i=t("p",null,[t("strong",null,"Reason"),o(": This is to make trailing slash behavior consistent: by default all routes allow a trailing slash. It can be disabled by using the "),t("code",null,"strict"),o(" option and manually appending (or not) a slash to the routes.")],-1),u=e('<h3 id="route-properties-encoding"><a class="header-anchor" href="#route-properties-encoding" aria-hidden="true">#</a> <code>$route</code> properties Encoding</h3><p>Decoded values in <code>params</code>, <code>query</code>, and <code>hash</code> are now consistent no matter where the navigation is initiated (older browsers will still produce unencoded <code>path</code> and <code>fullPath</code>). The initial navigation should yield the same results as in-app navigations.</p><p>Given any <a href="/api/#routelocationnormalized">normalized route location</a>:</p><ul><li>Values in <code>path</code>, <code>fullPath</code> are not decoded anymore. They will appear as provided by the browser (most browsers provide them encoded). e.g. directly writing on the address bar <code>https://example.com/hello world</code> will yield the encoded version: <code>https://example.com/hello%20world</code> and both <code>path</code> and <code>fullPath</code> will be <code>/hello%20world</code>.</li><li><code>hash</code> is now decoded, that way it can be copied over: <code>router.push({ hash: $route.hash })</code> and be used directly in <a href="/api/#scrollbehavior">scrollBehavior</a>&#39;s <code>el</code> option.</li><li>When using <code>push</code>, <code>resolve</code>, and <code>replace</code> and providing a <code>string</code> location or a <code>path</code> property in an object, <strong>it must be encoded</strong> (like in the previous version). On the other hand, <code>params</code>, <code>query</code> and <code>hash</code> must be provided in its unencoded version.</li><li>The slash character (<code>/</code>) is now properly decoded inside <code>params</code> while still producing an encoded version on the URL: <code>%2F</code>.</li></ul><p><strong>Reason</strong>: This allows to easily copy existing properties of a location when calling <code>router.push()</code> and <code>router.resolve()</code>, and make the resulting route location consistent across browsers. <code>router.push()</code> is now idempotent, meaning that calling <code>router.push(route.fullPath)</code>, <code>router.push({ hash: route.hash })</code>, <code>router.push({ query: route.query })</code>, and <code>router.push({ params: route.params })</code> will not create extra encoding.</p><h3 id="typescript-changes"><a class="header-anchor" href="#typescript-changes" aria-hidden="true">#</a> TypeScript changes</h3><p>To make typings more consistent and expressive, some types have been renamed:</p><table><thead><tr><th><code>vue-router@3</code></th><th><code>vue-router@4</code></th></tr></thead><tbody><tr><td>RouteConfig</td><td>RouteRecordRaw</td></tr><tr><td>Location</td><td>RouteLocation</td></tr><tr><td>Route</td><td>RouteLocationNormalized</td></tr></tbody></table><h2 id="new-features"><a class="header-anchor" href="#new-features" aria-hidden="true">#</a> New Features</h2><p>Some of new features to keep an eye on in Vue Router 4 include:</p><ul><li><a href="/api/#addroute-2">Dynamic Routing</a></li><li><a href="/guide/advanced/composition-api.html">Composition API</a>\x3c!-- - Custom History implementation --\x3e</li></ul>',11);c.render=function(e,t,o,p,c,l){return n(),a("div",null,[r,s(" Learn more about relative links [in the cookbook](/cookbook/relative-links.md). "),i,s(" TODO: maybe a cookbook entry "),u])};export default c;export{p as __pageData};
